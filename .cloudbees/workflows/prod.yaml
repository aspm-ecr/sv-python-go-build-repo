apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: Go-thu-oct-9
          artifact-id: fb527fbb-678b-4c87-ae4f-6ad4b6c1b0e4
          target-environment: ecr-qa      